import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './UseReducerState';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <pre>===================== USE REDUCER =====================</pre>
    <UseReducerState />
    <pre>==========================================</pre>
  </React.StrictMode>
);

